{
  "name": "bisect-example-rescript",
  "dependencies": {
    "bisect_ppx": "^2.0.0",
    "rescript": "*"
  },
  "scripts": {
    "build": "rescript",
    "test": "rm -f *.coverage && node ./lib/js/hello.js",
    "clean": "rescript clean"
  }
}
