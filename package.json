{
  "name": "bisect-example-rescript",
  "dependencies": {
    "bisect_ppx": "^2.6.3",
    "rescript": "9.1.4"
  },
  "scripts": {
    "build": "rescript build -with-deps",
    "test": "rm -f *.coverage && node ./src/hello.bs.js",
    "clean": "rescript clean"
  }
}